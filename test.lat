int foo(int p1, int p2)
{
    {
        int x = readInt();
        printInt(x);

        x = readInt();
        printInt(x);

        x = readInt();
        printInt(x);

        return 0;
    }

    int x = 10;
    {
        int y = 12;
        int w = (x + y) - (y - x);
    }
    int e = x * x;

    printInt(e);
    x + readInt();

    int retval = e;
    return retval;
}

// int bar(int i)
// {
    // int v = i +3;
    // return v;
// }

int main() { return 1; }
