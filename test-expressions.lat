int main()
{
    // int x, y = 5;
    // string str = "foo";

    // int q = x + str;
    // int z = x + ww;

    // 2 = 3;

    int x;
    x = true;

    // "foo" + "foo" == "foofoo";
    // x; // EVar
    // y; // EVar

    // 1 <= 4;

    // TODO: EClMem
    // TODO: EArrApp
    // TODO: ENew
    // TODO: ENewArr
    5123; // ELitInt
    "foobar"; // ELitString

    true;
    false;
    // null;

    {
        -443; // ENeg
        !false; // ENot
        // (boolean)(123); // ECast
        12 * 34; // EMul
        44 / 4; // EMul
        19 % 6; // EMul
        11 + 22; // EAdd
        71 - 9; // EAdd
    }

    int x = 4, y;
    string foo, bar = "hello darkness";
    boolean b = true, c, d;

    (1 < 3 || 5 > 9) && (2 < 4 || 6 > 10 || 7 < 11) && (2 > 5 - 2 * 2);

    // fooz(23 + 7 + 88 / 4); // EApp
    // foos("www" + "qq", (1 + 2) * 3 * 4 % 2);
}

/* int fooi(int i)
{
    return i * 8 - 3;
}

string foos(string s, int i)
{
    return s + "q";
} */
